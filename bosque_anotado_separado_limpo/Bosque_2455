(FRASE CP464-2 (np (prop Christina_Murphy)
                       ({)
                       («)
                       (np (prop Irish_Times))
                       (»)
                       (})))
