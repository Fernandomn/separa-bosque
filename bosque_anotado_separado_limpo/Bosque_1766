(FRASE CP339-2 (fcl (np (pron-det Cada)
                                 (n caso))
                        (vp (v-fin é))
                        (np (art um)
                               (n caso))))
